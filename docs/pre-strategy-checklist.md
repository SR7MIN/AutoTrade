# 策略接入前审核清单

## 代码中已实现

- [x] 全部和部分 `reduceOnly` 平仓，以及平仓后的清理/数量同步。
- [x] 已有持仓补保护，以及安全替换止损/止盈。
- [x] 持久化意图、订单、事件、成交、控制、命令、账户快照和 K 线。
- [x] 订单终态不可回退，重复事件幂等。
- [x] `ALGO_UPDATE` 实时状态迁移及 `TRADE_LITE` 成交补全。
- [x] 未知执行结果有限对账，不盲目重试交易请求。
- [x] 单一写入进程和持久化人工命令队列。
- [x] 用户数据流续期、重连、主动轮换和 REST 重新同步。
- [x] 持仓归零清理保护单，部分成交/平仓同步保护数量。
- [x] 持久化人工/自动开仓锁和账户级硬性风控。
- [x] 保留风险降低路径的本地限频保护。
- [x] 收盘 K 线契约、去重和断档恢复。
- [x] 结构化脱敏日志和持久化告警。
- [x] 不可变、带来源且会过期的 `EntryIntent` 策略边界。
- [x] 账户变更的预览/执行分离。

## 仍需部署验收

- [ ] Testnet 多空完整生命周期，包括部分平仓。
- [ ] 在入场和挂止损之间杀死进程，验证安全恢复。
- [ ] 成交期间断开用户数据流，验证 REST 恢复。
- [ ] 在预发布环境注入 429、503、时间偏移及重复/乱序事件。
- [ ] Testnet daemon 连续监督运行至少 72 小时。
- [ ] 配置外部告警和服务管理器自动重启。
- [ ] 生产 API Key 按权限和 IP 白名单限制。
- [ ] 完成地区/法律、密钥轮换、备份和事故响应审核。
- [ ] 主网先运行不具备下单权限的行情 Shadow 模式。

## 工程验证策略进度

- [x] UTC 历史范围分页回补、幂等存储和 K 线缺口报告。
- [x] 无执行依赖的不可变 `StrategySignal`、EMA 和 Wilder ATR。
- [x] 下一根 K 线成交、保守同柱处理和成本模拟的离线回放。
- [x] 实时 Shadow Runner、游标恢复、虚拟状态和独立决策日志。
- [x] `StrategySignal` 到 `EntryIntent` 的严格 Testnet 执行适配器。

在所有部署项目都有证据、且账户仍锁定主网前，不应批准任何交易策略。
